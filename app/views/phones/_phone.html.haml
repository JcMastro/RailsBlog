%table
  %thead
    %tr
      %th Phone type
      %th Number
      %th{:colspan => "3"}
  %tbody
    - @contact.phones.each do |phone|
      %tr
        %td= phone.phone_type
        %td= phone.number
        %td= link_to 'Destroy', contact_phone_path(@contact, phone),data: { |
            turbo_method: :delete,                                  |
            turbo_confirm: "Are you sure?" }                        |
